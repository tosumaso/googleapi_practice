(function(){
  let pos = new google.maps.LatLng(
   <%= @latlng[0] %>, //latitude
   <%= @latlng[1] %>  //longitude
  );

  map.setCenter(pos)
  let marker = new google.maps.Marker({
    map: map,
    position: pos
  });
})()